import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function SubmittedWorkDaysListFooter({ workDays }) {
  return (
    <View>
      <Text>SubmittedWorkDaysListFooter</Text>
    </View>
  );
}

const styles = StyleSheet.create({});

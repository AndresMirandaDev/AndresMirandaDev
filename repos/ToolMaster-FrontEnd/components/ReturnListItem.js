import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function ReturnListItem({ data }) {
  return (
    <View>
      <Text>ReturnListItem</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
